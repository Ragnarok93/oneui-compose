name: One UI 8 Compose demo APK

on:
  push:
    branches:
      - feature/oneui8-sesl-compose-demo
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-demo:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Verify library and build demo
        run: ./gradlew :lib:testDebugUnitTest :lib:assembleRelease :demo:assembleDebug --stacktrace

      - name: Upload demo APK
        uses: actions/upload-artifact@v4
        with:
          name: oneui-compose-oneui8-demo-debug
          path: demo/build/outputs/apk/debug/demo-debug.apk
          if-no-files-found: error
