name: Stargazers connected parity test

on:
  push:
    branches:
      - feature/oneui8-compose-components
    paths:
      - .github/workflows/stargazers-connected.yml

permissions:
  contents: read

jobs:
  connected-stargazers:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run Stargazers swipe parity on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          target: google_apis
          disable-animations: true
          script: ./gradlew :demo:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=org.oneui.compose.demo.StargazersSwipeUiParityTest --stacktrace
